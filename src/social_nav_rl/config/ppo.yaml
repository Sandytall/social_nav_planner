# PPO hyperparameters (Stable-Baselines3). Reasonable starting values for the continuous
# social-nav control task; tune from the training logs. Only SB3-accepted keys are read.
n_steps: 2048
batch_size: 256
n_epochs: 10
gamma: 0.99
gae_lambda: 0.95
learning_rate: 0.0003
ent_coef: 0.0
vf_coef: 0.5
clip_range: 0.2
max_grad_norm: 0.5
